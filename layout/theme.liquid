<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    
    {{ content_for_header }}
  </head>

  <body>
  
    {% section 'header' %}
    <main>
        {% section 'hero-banner' %}
        {{content_for_layout}}
        </main>
  </body>
</html>
